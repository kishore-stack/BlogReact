
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: blog-app
  labels:
    app: backend
spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP